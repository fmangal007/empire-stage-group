# Empire Stage Group

Cloudflare build settings:

- Build command: `npm run build`
- Deploy command: `npx wrangler deploy`
- Production branch: `main`
- Root directory: leave blank

Upload every file in this project to the root of `fmangal007/empire-stage-group`, commit to `main`, then retry the failed Cloudflare deployment.
